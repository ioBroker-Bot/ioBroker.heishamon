# Node.js / TypeScript
node_modules/
dist/
build/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# TypeScript build info
*.tsbuildinfo

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Coverage / test output
coverage/
.nyc_output/

# Captured protocol dumps from the heat pump or simulator (potentially large, may contain secrets in future)
captures/
*.cap
*.pcap

# Local-only scratch
scratch/
tmp/

# Claude Code local data
.claude/

# HeishaMon-Upstream-Snapshot — siehe README ("Lokales Setup").
# Nicht im Repo, weil HeishaMon keine explizite Lizenz hat (vgl.
# vendor/heishamon-upstream/LICENSE-NOTE.md). Der Snapshot wird lokal
# zur Referenz gehalten, fließt aber nicht in den Build/die Tests ein —
# alle Test-Fixtures sind unter packages/heishamon-protocol/test/fixtures/
# als JSON eingecheckt.
vendor/heishamon-upstream/
